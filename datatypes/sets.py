# set -> unordered and unchangeable , doesn't supports duplicates 
# add/remove operation only support
# sets -> we can store only primitive data types in the sets data, can't store {} and []
#         sets uses the hash the data reference we can't hash the mentioned data 

sets = {12, True, None}

print(sets[-1])
